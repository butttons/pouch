# pouch examples

These are runnable integration tests that demonstrate common CMS patterns. Each file creates its collections, seeds sample content, and runs queries to show how relations, media, and filters work.

Run the full suite with:

```sh
pnpm test
```

Run a single example:

```sh
pnpm test test/examples/blog.test.ts
```
